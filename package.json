{
  "name": "we-api",
  "version": "1.6.0",
  "description": "关于微信API的封装，Model Style，并且支持 Typescript",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "pnpm run build",
    "test": "vitest run --test-timeout=10000",
    "test:watch": "vitest --test-timeout=10000",
    "example": "ts-node examples/article.ts",
    "cov": "vitest run --coverage --test-timeout=10000"
  },
  "author": "shanyue",
  "license": "ISC",
  "packageManager": "pnpm@11.23.0",
  "dependencies": {
    "axios": "^1.6.8",
    "formstream": "^1.3.1",
    "midash": "^1.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "@vitest/coverage-v8": "^1.5.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5",
    "vitest": "^1.5.0"
  }
}
